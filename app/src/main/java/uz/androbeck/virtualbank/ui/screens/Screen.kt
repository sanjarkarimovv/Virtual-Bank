package uz.androbeck.virtualbank.ui.screens

enum class Screen {
    LOGIN, REGISTRATION
}

enum class HomeComponents {
    Cards, Payments, LastTransfers, PaymentForPhoneNumber, FinancesService, ForAdvertising;
}